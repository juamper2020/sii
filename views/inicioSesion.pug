<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title SII - Iniciar sesión
        link(rel="stylesheet", href="/css/bootstrap.min.css")
    body
        br
        div.row
            div.col-sm-4.offset-sm-4
                center
                    h1(align="center" style="color:red") SII
                br
                div.card
                    div.card-header Inicio de sesión
                    div.card-body
                        if error
                            div.alert.alert-danger= error
                        form(action="/autenticarUsuario" method="POST")
                            div.form-group
                                label(for="txtCorreoElectronico") Correo electrónico
                                input.form-control(type="email" name="txtCorreoElectronico")
                            div.form-group
                            div.form-group
                                label(for="txtContrasenia") Contraseña
                                input.form-control(type="password" name="txtContrasenia")
                            div.form-group.form-check
                                input.form-check-input(type="checkbox" id="chkMantenerSesion")
                                label.form-check-label(for="chkMantenerSesion") Mantener sesión iniciada
                            button.btn.btn-primary(type="submit") Iniciar sesión
                        br
                        center
                            a(href="/verRecuperarContrasenia") <strong>Recuperar contraseña</strong>
                        center
                            a(href="/verFormularioRegistro") <strong>Registrarme</strong>
                        
        